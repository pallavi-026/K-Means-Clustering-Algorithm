# K-Means-Clustering-Algorithm
K-Means Clustering Algorithm is an unsupervised machine learning technique used to group similar data points into K distinct clusters based on their features. It iteratively assigns data points to the nearest centroid and updates centroid positions until the clusters become stable.
